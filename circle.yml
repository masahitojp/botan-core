machine:
  java:
    version: oraclejdk8

dependencies:
  cache_directories:
    - "~/.gradle"

test:
  override:
    - ./gradlew dependencies
  pre:
    - cp gradle.properties.sample gradle.properties
